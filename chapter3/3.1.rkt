#lang racket
(cons 4
  (cons "art"
    (cons (list 5 6 3)
      (cons (cons "math" (cons (cons 8'()) (cons 99 '())))
        (cons 100
          (cons "we"
            '()
          )
        )
      )
    )
  )
)
